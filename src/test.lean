import data.real.basic

-- the game is: how quickly can we get the user playing the game.
-- The answer to this level is "change `sorry` to `refl`.

example : (37 : ℝ) = (37 : ℝ) :=
begin
  sorry
end
